import { PillarManager } from '@/modules/routines/components/PillarManager';

export default function Page() {
  return (
    <main style={{ background: '#000', minHeight: '100dvh' }}>
      <PillarManager />
    </main>
  );
}
